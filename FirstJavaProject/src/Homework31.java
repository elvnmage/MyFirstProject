/* Task 3.1
 * Generate array of random integers between 50 and 150 
 * (easy option - size is fixed and equals 100, 
 * more complex option - user should input amount of items 
 * he/she wants to have in array) and count amount of number 
 * “100” in that array. If there is no even 1 “100" 
 * please inform user about that.
 */
//Generate array of random integers between 0 and 150 (easy option - size is fixed and equals 100, more complex option - user should input amount of items he/she wants to have in array) and count amount of number “100” in that array. If there is no even 1 “100" please inform user about that.

import java.util.Random;

public class Homework31 {

	public static void main (String args[]) {
		
//		int randomNum = rand.nextInt((max - min) + 1) + min;
		

//		Random rand = new Random();

//		int n = rand.nextInt(150) + 50;
	
		Random rand = new Random(); 
		int value = rand.nextInt(150); 
		
		
		int i = 0;
		int counter = 0;
		
	for (i=0; i<4; i++) {
	
	}
	
		
		System.out.println (value);
	}
}
